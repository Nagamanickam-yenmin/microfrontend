import React, { useEffect } from "react";  
import { RootState } from "../../type/storeType";
import Button from "../../components/button";
 
const Login = () => {
  // const count = useSelector((state: RootState) => state);

  // useEffect(()=>{
  //   console.log(count,"count")
  // })
 
  return (
   <>
   <p>Login</p> 
   </>
  );
};

export default Login;
